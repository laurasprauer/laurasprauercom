import React from 'react';
import ReactDOM from 'react-dom';

// import components
import LauraSprauerCom from './laurasprauercom.jsx';

ReactDOM.render(<LauraSprauerCom />, document.getElementById('website'));
